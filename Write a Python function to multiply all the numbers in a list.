if __name__ == '__main__':
    import math
    num_list = [1,2,3,4]
    def multiplier(num_list):
        result = 1
        for num in num_list:
            result = result * num

        return result
    
    print(multiplier(num_list))
